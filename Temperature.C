#include<stdio.h>
#include<conio.h>

int main()
{
int c,sume=0,sum0=0;
for(c=1;c<=100;c++)
{
    if(c%2==0)
    {
        printf(" The sum of even numbers %d : ");
    }
    else
    {
     printf(" The sum is odd %d : " );
    }
}
return 0;
}